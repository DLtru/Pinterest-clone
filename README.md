# Pinterest-clone

Pinterest clone built with Vue (CDN) featuring a wallpaper gallery powered by the Pexels API.

## Run locally

Start a static server from the project root:

```bash
  npx serve .

```

A local server avoids CORS restrictions that block API requests when opening the HTML file directly via the `file://` protocol.

Then open `http://localhost:3000` in your browser.
